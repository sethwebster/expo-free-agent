# Expo Free Agent Controller Configuration

# API Key (minimum 16 characters)
# Used by workers and CLI to authenticate with controller
CONTROLLER_API_KEY=your-secure-api-key-here-minimum-16-chars

# Base VM Image ID
# Tart template name that workers will clone for builds
# Default: expo-free-agent-tahoe-26.2-xcode-expo-54
BASE_IMAGE_ID=expo-free-agent-tahoe-26.2-xcode-expo-54

# Optional: Override server port (default: 3000)
# PORT=3000

# Optional: Override storage paths
# DB_PATH=./data/expo-agent.db
# STORAGE_PATH=./data/storage
